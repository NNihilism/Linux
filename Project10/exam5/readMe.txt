验证父子进程是否共享文件指针或是拥有自己各自独立的指针体系


fork:
子进程复制了文件描述符，而对于文件描述符相关的文件表项(struct file结构),则采用共享的方式,因此会出现交替写入的情况（共享同一组偏移）

vfork:
由于子进程是在父进程的空间中运行的，资源共享 
且父进程在子进程未调用_exit或者exec之前是不会被调度运行的,因此文档中发现子进程先完成写操作，接着才是父进程完成操作。
注意子进程的结束方式，不要释放掉父进程的资源！
